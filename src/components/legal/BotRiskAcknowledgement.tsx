import {useState} from 'react'
import {Link} from 'react-router-dom'
import {Bot,TriangleAlert} from 'lucide-react'
export default function BotRiskAcknowledgement(){const [checks,setChecks]=useState([false,false,false]);const update=(index:number,value:boolean)=>setChecks(current=>current.map((item,i)=>i===index?value:item));return <section className="bot-acknowledgement"><div><Bot/><span><b>Separate bot activation acknowledgement</b><small>AOS Robot · pre-release concept · activation disabled</small></span></div>{['I understand automated tools can fail and do not guarantee profit.','I remain responsible for configuration, monitoring and emergency intervention.','I have read the complete Trading Risk Disclosure.'].map((item,index)=><label key={item}><input type="checkbox" checked={checks[index]} onChange={e=>update(index,e.target.checked)}/>{item}</label>)}<button className="button" disabled><TriangleAlert/>Activation requires regulatory, legal and platform approval</button><Link to="/risk-disclosure">Read the bot-risk section</Link></section>}
